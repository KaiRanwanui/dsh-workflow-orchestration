# 工作流数据管理需求澄清记录

- **澄清时间**：2026-09-01 起（逐组进行）
- **原始需求**：`工作流数据管理原始需求.md`（25 条，四组）
- **状态**：A / B / C / D / E 五组**均已澄清**；剩余 2 个开放点待拍板（见下）；澄清完成后回写原始需求文档，并作为迭代计划刷新的输入。
- 本文档记录逐条决策与语义修订，澄清全部完成后回写更新原始需求文档，并作为迭代计划刷新的输入。

---

## A 组：预定义目录（需求 1、2、4 地基）

| # | 决策 | 内容 |
|---|---|---|
| A1 | ✅ 已定 | **全局唯一目录**（DSH 用户级，如 `~/.dsh/workflow-agent/`），所有工作区编排会话共享；不做工作区本地覆盖 |
| A2 | ✅ 已定 | **内建模板物化到预定义目录**（插件代码不再内嵌模板源）；**升级同名直接覆盖**，版本控制未来交给 git 仓 |
| A3 | 🔶 半定 | **增加 `${skills}` 目录变量**；但"技能资源相对路径的解析基"存在兼容性权衡（见开放点 1） |
| A4 | ✅ 已定 | 文件引用统一支持相对路径，解析规则：**以 workspace 为根优先，找不到再回退预定义目录**（两级查找链） |

**附带决策（对前轮冲突点的裁决）**：

- 需求 10（STOPPED 实例可编辑未执行任务）：**优先级放低**，先不支持启动后修改实例设置，后续适时实现。→ 前轮冲突 ①（resume 不重读 instance.yaml）随之延后。
- 需求 20（目录变量）：**采用 `${param}` 同款语法**，即 `${workspace}`、`${wf_dir}`、`${skills}`，与现有 `${param}`/`${item}` 同一注入正则，引擎解析展开期注入绝对路径。
- 需求 4（技能执行目录）：实例定义文件所在目录是否作为 process 技能执行目录——**待讨论**（见开放点 1）。

## B 组：实例编辑与状态约束（需求 5-11、14）

| # | 决策 | 内容 |
|---|---|---|
| B1-形态 | ✅ 已定 | 编辑面板放 **DAG 页下方双栏**：左侧任务列表（含类型/状态），右侧选中任务属性表单；DAG 图初期**只读**（不支持鼠标操作），图元点击快速定位可后置。实例管理入口：弹专题窗或新增子页签（二选一见开放点 2） |
| B1-技能选择 | ✅ 已定 | **优先实现下拉列表**（扫预定义目录 skills/，选现有技能）；"自定义"列（用户现场编写技能）**优先级降低** |
| B2 | ✅ 已定 | **实例名称为第一展示字段**（比 ID 友好），各处列表/DAG 显示"名称+ID"；instanceId 永不变 |
| B3 | ✅ 已定 | 允许修改工作流级 **params**（简单键值表即可，参数数量有限） |
| B4 | 🔶 退后 | "停止后编辑未执行任务"存在**重入 vs 清理**二择一：支持任务在已有产物基础上重入，还是每次执行先清理产物。未想清楚，**退后**；未来可能两种都要、可配置 |
| B5 | ✅ 已定（修订原需求） | 内联指令**不存实例目录**，改为**保存到预定义目录 skills/ 下**作为共享技能文件（简单、可复用）；实例级技能 copy 编辑留待未来 |
| B6 | ✅ 已定 | RUNNING 实例编辑入口**可见但禁用**（提示"运行中不可编辑"） |

**需求语义修订（相对原始文档的 delta）**：

1. 需求 11/14 原文"这份指令仅保存在工作流实例目录中" → 修订为"保存到预定义目录 skills/ 下作为可复用技能"（B5）。
2. 需求 10 原文 → 降级为后续版本（B4 退后，与需求 9 合并为"启动后一律不可编辑"，RUNNING 可见禁用）。
3. 需求 6 编辑范围中"工作流实例名称、总并发数量；任务 input/output 路径、process 技能（下拉）、gateChecker 技能（下拉）、门禁重试次数、并发数量"维持，另加**工作流级 params**（B3）。

---

## 开放点（待拍板，仅剩 2 项）

1. **技能资源查找与执行目录**（需求 4/20/21/22 交叉）：
   - 现状事实：子会话执行时工作目录=workspace（需求 22）；技能文本里的相对路径默认靠 Agent 自行推断解析（即 OpenCode/Claude 的实际兼容行为）。
   - 两种取向：**兼容优先**（不给显式变量，靠 Agent 推断，第三方技能零改动可用）vs **确定优先**（显式 `${skill_dir}`/`${skills}`，自家技能好写但第三方技能不受益）。
   - 候选折中（待拍板）：两者并存——派发时在子会话 prompt 附加一行"本技能全文来自 `<技能目录绝对路径>`"（不改技能文本，提升兼容确定性），同时提供 `${skill_dir}`/`${skills}` 变量给自家技能显式引用。
2. **实例管理入口**：弹专题窗 vs 新增子页签（E 组实例列表/下载/删除都放这里；建议子页签——信息容量大、未来归档管理同处）。
3. ~~B4 重入 vs 清理~~：已随 B4 退后，未来可配置，不在本轮范围。

## C 组：数据传递与 items（需求 16-18、20）

| # | 决策 | 内容 |
|---|---|---|
| C1 | ✅ 已定 | `${workspace}` / `${wf_dir}` / `${skills}` 采用 `${param}` 同款语法，引擎解析展开期注入绝对路径 |
| C2 | ✅ 已定 | items 提取**显式声明优先、扩展名推断兜底、行文本向后兼容**（确定性优先，未来会对 items 文件格式提出要求）。支持格式：**markdown 列表与表格行**（表格每行=item，列名为字段）；**JSON/YAML 数组与并列对象**（含 JSON Lines 式并列对象）。**对象 item 的标量注入语义**：`${item}` 默认优先取 **ID 或编号字段，其次名称字段**，都没有则按默认顺序编号；`${item.字段名}` 支持且仅限提供了字段名的表格行 / JSON / YAML 对象 |
| C3 | ✅ 已定 | 门禁子会话同时接收该任务的 **inputs + outputs** 全部路径（现仅 outputs，需改协议） |

## D 组：技能兼容（需求 21、22）

| # | 决策 | 内容 |
|---|---|---|
| D1 | ✅ 已定 | **不兼容 OpenCode/Claude 特有的工具与指令**。DSH 子会话执行技能（技能=指令文本+子会话自带工具）；需要外部 Agent 特有能力时，**倾向启动 OpenCode 等 Agent 作为子执行体**——落点=schema 已预留的 `external-agent` 任务类型（仅 id+agent 必填，解析识别，协议/派发未实现，待后续迭代） |
| D1-事实 | 📌 技术事实 | ① DSH 子会话架构上可嵌套（树），但**控制通道仅限直接父子**（followup/注入跨层被拒，跨层只能 interrupt）——孙会话只能经子会话转达；② DSH 原生 skill 机制（技能目录+skill 工具）存在，但工作流走"读技能全文粘贴"路线，未用原生机制 |
| D2 | ✅ 已定 | 技能清单下拉只列**名称+版本**（skill metadata 有版本才显示）；描述文字量不受控，不做默认展示；浮动卡片展示描述可后置 |

## E 组：归档与实例管理前台（需求 23-25）

| # | 决策 | 内容 |
|---|---|---|
| E1 | ✅ 已定 | 实例列表列集=名称、ID、状态（阶段+派生）、孤儿标记、任务进度、创建时间（现有提议即够） |
| E2 | ✅ 已定 | 打包格式 **zip**；**多选打一个包**（包内按实例分目录） |
| E3 | ✅ 已定 | **RUNNING 不允许删除**（其余阶段可删）；删除时**自动解绑会话** |
| E4 | ✅ 已定 | 列表分**活动实例 / 归档实例**两段；归档段同样支持下载/删除 |

---

## 开放点（待拍板，仅剩 2 项）
