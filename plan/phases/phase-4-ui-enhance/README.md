# 阶段 4 · 现有功能修复（Iter-31 ~ 43）— 阶段总结

> 本文件为阶段 4 收尾总结（写一次后封版不回改）。迭代级细节见 `iterations/`。

- **时间**：2026-09-16 ~ 2026-09-20
- **DSH 基线**：0.1.5-rc.2（不变）
- **交付**：host **v0.26.50**（单包形态延续；client 版本号并入 host 包内递增）
- **测试**：602 单测全绿 + verify-client-bundle（求值 + load + factory + **apply 冒烟**）双门
- **状态**：✅ 已完成（13 个迭代全部验收关闭）

## 1. 交付脉络（13 个迭代）

| 组 | 迭代 | 主题 |
|---|---|---|
| 缺陷闭环（8 个） | Iter-31~38 | 状态重置语义/实例完整性与采纳关口/门禁真实执行/进度分档/轮次展示优化/停止链路稳定化/错误链可视化/定义全文编辑 |
| 表单与数据流 | Iter-36 补全 + Iter-38 | 表单补全（gate/retries/timeout/params 单轨化——params 并入 instance.yaml 由 patch.params 承载，/wf/instance-params 路由退役） |
| 交互直观性 | Iter-39~41 | 会话页签门控（仅编排主会话显示）、RUNNING 运行视觉（呼吸脉冲+视口外自动居中+门禁角点）、节点详情卡+组节点成员清单+文件预览 |
| 数据源重构 | Iter-42 | DAG 结构层=instance.yaml（编辑保存即刷新所见即所得）+状态层=state 叠加（执行字段优先定义值回退）；CREATED 定义态 DAG；技能读取统一「工作空间→.dsh 两级链」 |
| 主题适配 | Iter-43 | HTML 层 token 常量 T 接入 DSW 皮肤 CSS 变量（--dsw-alias-*，带原色 fallback）；状态语义色保留字面；验收期浅色主题四项颜色微调+按钮统一中文+选中描边三型统一蓝 |

## 2. 验收期事故与教训（本阶段最重要的工程资产）

| 事故 | 根因 | 固化 |
|---|---|---|
| T 常量静默丢失 → apply 白屏（v0.26.44） | 多锚点 replace 无断言，锚点漂移静默 no-op | verify-client-bundle 增 **apply 冒烟门**（stub ctx 实际执行 apply）；替换必须断言命中 |
| 多锚点脚本中途断言失败整批丢失（v0.26.46 颜色修复误报完成） | 脚本写盘集中在末尾，中途失败全丢 | **逐项写盘**（每替换独立 write）；逐标记核验源码+部署产物 |
| 部署断链 7 轮未察觉（v0.26.35~41） | ① sed 基准手写错位静默不命中；② pnpm 对 file: tgz 的 store 缓存键不含内容 | manifest 改 **python 正则从 repo 版本派生**；安装改 **tar 直解**（绕过 pnpm store）；部署后 python 逐标记核验产物 |
| 诊断探针残留 | 验收期诊断版多次重建 | 收尾清理动作进收尾清单（本阶段已清零） |

## 3. 遗留与后续

| 项 | 说明 | 去向 |
|---|---|---|
| **权限矩阵对齐专项** | 结构编辑仅 CREATED 可改（Iter-28 矩阵）vs 所见即所得/params 单轨化的演进——用户提出找时间专门对齐修改场景 | **用户发起的待专项** |
| /wf/skill 围栏收口（64KB/二进制检测） | 现状任意路径直读 | 独立迭代候选 |
| SVG 深色画布在浅色主题下的观感 | DagCanvas SVG fill/stroke 为 attribute 不支持 var()，已用 style 覆盖解决主要面；如需全 SVG 主题化走 class+CSS 路径 | 按需迭代 |
| 下一阶段 | 队列待立项（权限矩阵对齐专项优先） | 待用户发起 |
