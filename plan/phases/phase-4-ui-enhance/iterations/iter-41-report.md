# Iter-41 报告 — 节点详情与文件预览

- **状态**：✅ 完成关闭（2026-09-19，用户真机验收通过）
- **阶段**：阶段 4（现有功能修复/补全）
- **版本**：host `v0.26.29 ~ v0.26.32`（29 主交付 / 30 查看路径客户端预拼接 / 31 服务端两级链 + client wsRoot / 32 移除客户端预拼接——解析统一收归服务端；单包）
- **测试**：602 单测全绿（无回归）+ bundle 求值验证
- **提交**：见 git log（1206e24）

## 1. 交付件（方案=iter-41-design.md；用户澄清：详情卡=DAG 正下方独立卡非 SVG 内嵌/非编辑器内；成员详情=完整任务卡+迭代上下文行）

| # | 交付件 | 实现 |
|---|---|---|
| 1 | 任务节点详情卡 | 点击节点 → DAG 下方展开：基础（id/名称/类型/状态/retries/dependsOn）· 数据流（inputs 按键分组/outputs，路径可点击）· 处理器与门禁（processor+查看技能 / gateChecker+查看 / gateResult 色点 / gateNote）；再点节点或 ✕ 收起；编辑器展开时让位 |
| 2 | 组节点成员清单卡 | 点击循环/并发组节点 → 成员清单（序号/状态色点/迭代项·名称/状态）；点成员行 → setSelectedId(成员id) 切成员完整详情 |
| 3 | 文件预览弹层 | 路径/processor/gateChecker 点击 → GET /wf/skill 通道只读弹层（720px modal，路径头+pre 正文+错误通道）；复用 /wf/skill 零新路由 |

## 2. 实施与验证

| 步骤 | 验证 | 结果 |
|---|---|---|
| 详情卡/成员卡/预览弹层实现 | node --check + 构建 + bundle 求值 | ✅ |
| 全量单测 | test-host | ✅ 602 全绿 |
| 发行部署 | 内容断言 + 部署产物 grep（detailCardEl 2/fileView 6/组成员 4） | ✅ v0.26.29 |
| 真机 | ✅ 验收通过（六条全过；技能读取语义统一为「工作空间没有则从 .dsh 读」，编辑页与详情卡同源） | |

## 5. 真机验收清单（用户 GUI：重启后台 + 强刷浏览器）

| # | 操作 | 通过标准 |
|---|---|---|
| 1 | 点击任务节点 | DAG 下方展开三分区详情卡；内容与快照一致 |
| 2 | 点击 inputs/outputs 路径 | 只读弹层展示文件全文；✕ 关闭 |
| 3 | processor「查看技能」/ 门禁「查看」 | 技能/门禁文件弹层展示 |
| 4 | 点击组节点 | 成员清单卡（状态色点/迭代项）；点成员行 → 成员完整详情（含迭代上下文行） |
| 5 | ✎ 展开编辑器 | 详情卡让位隐藏；关闭编辑器恢复 |
| 6 | 再点已选中节点 / ✕ | 详情卡收起 |

## 6. 范围控制

不含时间戳（已拍板）；无 64KB/二进制围栏（/wf/skill 现状直读，收口另立迭代）；不支持文件编辑。

## 7. 参考

- 设计：`iter-41-design.md`（踏勘：/wf/skill 任意路径直读可复用；快照字段齐备零服务端改动）

## 8. 补记：验收修复链（v0.26.30 ~ 32）与最终形态

验收期「查看技能」三轮修复，最终形态：**客户端传原始路径（相对/绝对原样）+ workspaceRoot，服务端两级链裁决（工作空间 → .dsh 预定义目录）**——编辑页与详情卡技能读取语义统一。

| 版本 | 改动 | 结果 |
|---|---|---|
| 0.26.30 | 客户端预拼接（相对→工作区绝对） | 工作区无此文件仍报错 |
| 0.26.31 | 服务端两级链 + client 附 workspaceRoot | 客户端预拼接产生的绝对路径**跳过链解析**（互相抵消） |
| 0.26.32 | 移除客户端预拼接，传原始路径 | ✅ 相对路径走链命中 .dsh；绝对路径直通 |

## 9. 遗留

| 遗留 | 去向 |
|---|---|
| 编辑页 openSkillView 改走服务端两级链（消除列表匹配分叉） | Iter-42 顺带 |
| /wf/skill 围栏收口（64KB/二进制检测） | 独立迭代候选 |
