# 阶段 4 · 完成验收清单（用户逐项验收用）

> 基线：DSH 0.1.5-rc.2，host **v0.26.51**（602 单测绿 + 求值/apply 冒烟门 + 部署核验门）
> 用法：逐条操作→对照预期。不通过项请记录（迭代号+现象+Console 报错），作为后续阶段/迭代输入。
> 每轮验收前：`systemctl --user restart dsh.service` + 浏览器 Ctrl+Shift+R。

## A. 缺陷闭环组（Iter-31~35，8 缺陷回归确认）

| # | 操作 | 预期 | 迭代 |
|---|---|---|---|
| A1 | RUNNING 实例面板等待/停止提示 | 无多余提示残留（wfStopHint 已删） | 31 |
| A2 | 模板选 verify-gate 创建并启动 | 三门禁任务独立子会话；gate-pass→DONE(G)、skip→SKIPPED、block→FAILED 含理由；状态条 G 值正确 | 32 |
| A3 | 实例目录存在但 begin 未完成（CREATED） | 面板显示「已创建未启动」状态卡与引导，非无限等待 | 33 |
| A4 | 门禁任务执行至 gate | gate-checker 实际执行；block 时 FAILED 含门禁结论；gateNote 反馈链可见 | 34 |
| A5 | 定义全文编辑 | 编辑器 YAML 源码模式 ↔ 表单双态切换；保存走语义校验（errors 非空不落盘） | 35 |

## B. 表单与数据流组（Iter-36~38）

| # | 操作 | 预期 | 迭代 |
|---|---|---|---|
| B1 | 编辑器查看 items-from 任务 | items-from 字段可编辑；只读项呈现；下拉配色可读 | 36 |
| B2 | 编辑器 params 区 | key-value 编辑器（模板默认值预填）；保存生效（instance.yaml params） | 37/38 |
| B3 | params 单轨化 | 无 /wf/instance-params 调用；params 保存走 /wf/instance-yaml patch.params；非 RUNNING 可改 params | 38 |

## C. 交互直观性组（Iter-39~41）

| # | 操作 | 预期 | 迭代 |
|---|---|---|---|
| C1 | 切换到非编排会话/子会话 | 无 Workflow 页签（完全隐藏）；编排主会话恢复 | 39 |
| C2 | RUNNING 实例 | 任务节点呼吸脉冲；loop/concurrent 组内 RUNNING 迭代组状态条与成员行 dot 脉冲；视口外自动滚入（手动滚动 3s 宽限） | 40 |
| C3 | 门禁任务 | 右上角门禁角点全程显示并随 gateResult 变色 | 40 |
| C4 | 点击任务节点 | 详情卡三分区（基础/数据流/处理器与门禁）；分栏标题 ▍加粗主色 | 41 |
| C5 | 组节点展开 | 成员清单；点成员行进成员详情+迭代上下文 | 41 |
| C6 | 点击 inputs/outputs 路径 | 文件预览显示**实例目录下实际产物**（执行态绝对路径） | 41/42 |

## D. DAG 数据源重构（Iter-42 核心）

| # | 操作 | 预期 |
|---|---|---|
| D1 | CREATED 实例查看 | 显示定义态 DAG（任务 PENDING 态；loop/concurrent 占位组「等待 items」） |
| D2 | CREATED 表单加任务/删任务/改 depends-on/改名 → 保存 | DAG **立即**跟随（不切页签）；节点名即时更新 |
| D3 | 已执行实例（STOPPED 等）改 depends-on 保存 | 结构层以定义为准——DAG 反映新依赖；状态叠加保留（执行字段不丢） |
| D4 | 删除已执行任务保存 | 节点消失 + 「执行状态属上一轮」黄色徽标；Reset 后徽标消失 |
| D5 | 详情卡「查看技能」（相对路径技能） | 返回 .dsh 中技能全文（两级链） |
| D6 | 编辑器「查看技能」 | 同上（与详情卡同一后台服务，行为一致） |

## E. 主题适配组（Iter-43）

| # | 操作 | 预期 |
|---|---|---|
| E1 | 深色主题面板全览 | 观感不劣化；DAG PENDING 边框深灰 #475569（与黑底区别清晰）；RUNNING 边框蓝 |
| E2 | 浅色主题面板全览 | 节点底色/文字/连线随主题；文字可读；弹层代码底色随主题 |
| E3 | 浅色主题绿/黄元素 | Start/完成状态绿加深醒目；Reset/等待 items 黄加深醒目 |
| E4 | 起终点图元 | 均深灰；起点实心、终点**圆环** |
| E5 | 选中任意类型图元 | 选中描边统一**蓝色**（无黑框） |
| E6 | 按钮排 | 全中文（▶ 启动/⏹ 停止/↻ 重置/▶ 恢复/+ 创建/✎ 编辑/📋 管理）、字号统一 12 |
| E7 | 下拉列表（技能/枚举/模板） | 全部选项文字清晰（固定浅底深字） |
| E8 | 详情分栏标题 | ▍加粗主色，与属性行明显区分 |

## F. 回归确认

| # | 操作 | 预期 |
|---|---|---|
| F1 | Start→执行→Stop→Resume→Reset 全流程 | 状态机流转正常，面板状态/按钮随动 |
| F2 | 归档实例 | 实例移出列表（管理页签） |
| F3 | 插件加载 | 服务启动无 "failed to apply" 报错（诊断探针已清零） |
