# 迭代方案模板

> 用法：复制本文件为 `<迭代名>-design.md`，填写后**提请用户确认**（团队约定 §A6：未确认不得开写代码）。
> 存放位置：开发期放在阶段目录（如 `plan/phases/phase-3-*/`），迭代收尾后与报告一同归档到该阶段 `iterations/`。

---

# 迭代设计 — <迭代名>

- **阶段**：阶段 N（<阶段名>）
- **日期**：YYYY-MM-DD
- **DSH 基线 / 项目版本**：DSH `<x.y.z>`；host `<v>` / client `<v>`

## 1. 背景与目标

- **为什么做**（当前问题 / 触发原因，附证据：文件、行号、实测输出）
- **做完后达成什么**（可验收的一句话目标）
- **不做什么**（显式排除项，避免范围蔓延）

## 2. 交付件

| # | 交付件 | 路径 | 说明 |
|---|---|---|---|
| 1 | | | |

## 3. 技术选型与替代方案

| 方案 | 做法 | 优点 | 代价 | 结论 |
|---|---|---|---|---|
| A（推荐） | | | | |
| B | | | | |

> 有多个可选路径时必须列出并给出推荐理由；**待用户拍板的决策点单列**（见 §6）。

## 4. 执行顺序与差分验证

按「一次只改一个维度」（§A2）拆分，每步给出**最小可观测验证手段**：

| 步骤 | 改动 | 验证手段 | 通过标准 |
|---|---|---|---|
| 1 | | | |
| 2 | | | |

## 5. 验证标准（完成线）

- [ ] 单测：`node code/scripts/test-host.js` 全绿（N 用例）
- [ ] 产物级：`node code/scripts/verify-client-bundle.js`（涉及 Client 时）
- [ ] 真机：重启 `dsh.service` → `/wf/list` 200 / 工具注册 / 面板可用（涉及 Host 时）
- [ ] 行为验收：<本迭代的具体行为项，需「agent 真实感知」而非仅状态变化>
- [ ] 文档：报告归档 + 阶段/状态文档按规则更新

## 6. 待用户拍板的决策点

| # | 决策点 | 选项 | 推荐 |
|---|---|---|---|
| 1 | | | |

## 7. 风险与回退

| 风险 | 影响 | 缓解 | 回退点 |
|---|---|---|---|
| | | | |

## 8. 工作量估计

<人天>；关键路径：<步骤>
