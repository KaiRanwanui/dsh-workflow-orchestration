# Iter-21 手工验证报告 v2 — Stop/Resume 经 Session 指令 + 采用刷新 + DAG 闪烁

> 用途：部署 Iter-21 热修（host v0.11.7 / client v0.5.7）后，由人工逐条执行并填写**验证结果**与**问题**。
> 填写约定：验证结果填 `PASS` / `FAIL` / `N/A`；问题列填简要现象或留空。
> 说明：v1（R1~R5）见 `iter21-verification-report.md`；本次聚焦 4 类改动：①Stop/Resume 经 session 注入指令 ②采用打开即刷新 ③DAG 闪烁根治确认 ④主要回归。

## 0. 部署前置（先完成）

- [ ] `systemctl --user restart dsh.service`（加载 host v0.11.7；刷新浏览器只重载 Client）
- [ ] 浏览器硬刷新（Ctrl+Shift+R，载入 client v0.5.7 bundle）
- [ ] 开**新的 workflow-orchestrator 会话**（system-prompt 变更需新会话才生效；旧会话不热更新）
- [ ] 用默认模板 `default-demo` 创建→Start（免改免参），作为执行载体
- [ ] 确认 `/wf/list` 正常 + 返回 `sessionState`

---

## A. Stop（经 session 指令——本轮核心）

| 编号 | 场景 | 操作 | 预期 | 验证结果 | 问题 |
|------|------|------|------|---------|------|
| A1 | Stop 生效 | workflow 执行中点「Stop」 | 实例 → STOPPED（DAG 停）+ **编排 session/agent 停驱**（不再产生新任务/子会话，会话停止推进） | | |
| A2 | 进度保留 | Stop 后观察 DAG | 已完成任务保留（DONE），非清空 | | |
| A3 | 消息注入 | DevTools / DSH 会话消息区查看 | 会话收到注入消息（"请停止工作流实例 …"） | | |

---

## B. Resume（经 session 指令——本轮核心）

| 编号 | 场景 | 操作 | 预期 | 验证结果 | 问题 |
|------|------|------|------|---------|------|
| B1 | Resume 续跑 | STOPPED 实例点「Resume」 | 实例 → RUNNING + **agent 续驱**（后续任务继续执行，DONE 保留） | | |
| B2 | 消息注入 | 会话消息区查看 | 会话收到注入消息（"请继续工作流实例 …"） | | |
| B3 | 已知约束 | 若此前用"停掉 DSH 会话"（agent idle）实现停止 | Resume 无法唤醒已停 session（需先恢复该会话再 Resume）——记录为**已知约束**，不算失败 | | |

---

## C. 采用（打开即刷新）

| 编号 | 场景 | 操作 | 预期 | 验证结果 | 问题 |
|------|------|------|------|---------|------|
| C1 | 采用池即时 | UNBOUND 会话点「采用」 | 采用池**立即**列出可用（sessionId==null）实例；点击即绑定本会话 | | |
| C2 | 空池提示 | 无 unbound 实例时 | 显示"当前没有未绑定实例，请先创建一个新实例" | | |
| C3 | 孤儿说明（已知） | 池为空但有"死会话绑定"的孤儿 | 孤儿暂不在池内（需 Iter-22/S3 recoverOrphan 解绑再列）——记录为已知，不算失败 | | |

---

## D. DAG 闪烁根治（本次 v0.5.6 已修复，确认）

| 编号 | 场景 | 操作 | 预期 | 验证结果 | 问题 |
|------|------|------|------|---------|------|
| D1 | 执行期稳定 | default-demo Start 后持续观察 DAG | DAG **不再**频繁重建/闪烁（稳定刷新节点颜色，无空白/抖动） | | |
| D2 | 消息流稳定 | session 响应/子会话创建期间观察 DAG | 面板稳定，局部状态（Start/Stop/Resume 按钮、弹窗）不丢失 | | |

---

## E. 回归（确认 Iter-21 R1~R5 + 本轮未破坏）

| 编号 | 场景 | 操作 | 预期 | 验证结果 | 问题 |
|------|------|------|------|---------|------|
| E1 | CREATED DAG 任务节点 | 创建实例（不 Start） | DAG 显示任务节点（PENDING），非仅起终点 | | |
| E2 | 子会话占位 | 打开 workflow 子会话 | 子会话面板显示占位"此会话不是 Workflow 编排会话" | | |
| E3 | 会话切换 | 同工作区切换会话 | 状态/按钮即时正确；`/wf/status` 非空 | | |
| E4 | BROKEN 反复 | 删 metadata→恢复→再删 | BROKEN 告警每次都出现；`/wf/list` 轮询不中断 | | |

---

## 汇总

| 组 | 通过 | 失败 | N/A | 备注 |
|----|------|------|-----|------|
| A | | | | |
| B | | | | |
| C | | | | |
| D | | | | |
| E | | | | |

**总体结论**：□ 全部通过　□ 存在需修复问题（见问题清单）

**问题清单**（编号 + 现象 + 复现步骤）：

## 其他问题

（留白，供人工填写本版新发现的问题）
