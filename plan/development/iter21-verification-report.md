# Iter-21 手工验证报告（v1 — v4 手测问题闭环：R1~R5）

> 用途：部署 Iter-21（host v0.11.6 / client v0.5.5）后，由人工在 Web UI 逐条执行并填写**验证结果**与**问题**两列。
> 填写约定：验证结果填 `PASS` / `FAIL` / `N/A`；问题列填简要现象或留空。
> 说明：本文件为 Iter-21 手工验证（对应 v4 手测问题 A4/A6/其他#1/#2/B2/D3）。S1/S3/S4 仍未修，见文末"遗留"节，不在本版验证范围。

## 0. 部署前置（先完成）

- [-] `systemctl --user restart dsh.service`（加载 host v0.11.6；刷新浏览器只重载 Client）
- [-] 浏览器硬刷新（Ctrl+Shift+R，载入 client v0.5.5 bundle）
- [-] 开**新的 workflow-orchestrator 会话**（同工作区可开 2 个，用于 B/C 切换测试）+ 一个**非 workflow 会话**
- [-] 确认 `/wf/list` 正常 + 返回 `sessionState`（含 `BOUND/UNBOUND/BROKEN/DONE`）
- [-] **默认模板可执行**：`default-demo`（3 任务并发+汇总+门禁，免改免参），可直接 创建→Start

---

## A. R1（A4）CREATED 态 DAG 显示任务节点

| 编号 | 场景 | 操作 | 预期 | 验证结果 | 问题 |
|------|------|------|------|---------|------|
| A1 | 新建实例预览 | workflow 会话点「创建」→ 选 `default-demo` → 创建（不 Start） | 面板 DAG 显示 **3 个任务节点**（write-spec / prep-data / integrate，PENDING 灰）+ start/end；**不再是只有起点/终点** | 通过 | |
| A2 | 节点名/依赖 | 观察 A1 的 DAG | 3 个节点名称/依赖关系正确（write-spec 与 prep-data 并列，integrate 依赖二者） | 通过 | |

---

## B. R2（A6）子会话占位

| 编号 | 场景 | 操作 | 预期 | 验证结果 | 问题 |
|------|------|------|------|---------|------|
| B1 | 子会话门控 | 在 workflow 会话 Start `default-demo`，执行中产生 subagent；在 DSH 侧边栏打开该 **subagent 会话** | subagent 会话的面板显示占位 **「此会话不是 Workflow 编排会话，无监控面板。」**；**无** +/采用/DAG | | |
| B2 | 主会话不受影响 | 切回 workflow 主会话 | 主会话仍显示完整工作流面板（DAG/按钮） | 通过 | 问题：DAG界面渲染会闪烁，不是很频繁，跟Session反应同步 |

---

## C. R3（其他#1）会话切换状态即时正确

| 编号 | 场景 | 操作 | 预期 | 验证结果 | 问题 |
|------|------|------|------|---------|------|
| C1 | 会话 B UNBOUND | 同工作区（workflow_test_ws）开第 2 个 workflow 会话 B（未绑定实例），切换过去 | B 面板显示 **空态 + 「创建」「采用」**（不显示 Start/Stop/Reset，无残留上一会话的 Start） | 通过 | |
| C2 | 会话 A BOUND | 切回已绑定实例的会话 A | A 面板显示其绑定实例 DAG + 控制按钮（状态正确，不再错乱） |通过| |
| C3 | /wf/status 非空 | 查看 DevTools Network 中 `/wf/status` 响应（已绑定会话） | 返回正常状态（非 null）；`stateData` 非空  | 通过 | |
| C4 | 新会话即时 | 新建 workflow 会话（不刷新浏览器）观察按钮 | 无需 F5 即显示正确按钮（+ / 采用 或 依绑定态），不长时间停留错误态 | 通过 | |

---

## D. R3（其他#2）采用池即时列出

| 编号 | 场景 | 操作 | 预期 | 验证结果 | 问题 |
|------|------|------|------|---------|------|
| D1 | 采用池即时 | 在 UNBOUND workflow 会话点「采用」 | 弹窗**立即**列出所有未绑定（sessionId==null）实例；点击即绑定本会话；若确实无未绑定实例才提示"当前没有未绑定实例" | 未通过 | 问题：点击无反应；点击创建新建wf实例并刷新出DAG后，弹出采用列表界面 |
| D2 | F5 后采用 | 新会话 F5 后点「采用」 | 采用弹窗正常打开并列出孤儿/未绑定实例（不再无反应/空窗） | 未通过 | 同上 |

---

## E. R4（B2）BROKEN 反复识别

| 编号 | 场景 | 操作 | 预期 | 验证结果 | 问题 |
|------|------|------|------|---------|------|
| E1 | 首次 BROKEN | 删除绑定实例的 `metadata.json` | 面板显示 **「⚠ 环境异常，需新建 workflow 会话」** 告警卡（含原因）；无 创建/采用/控制按钮 | 通过 | |
| E2 | 恢复 | 恢复/重建实例后刷新 | 面板恢复 正常态（UNBOUND 创建/采用 或 BOUND DAG） | 通过 | |
| E3 | 二次 BROKEN（难复现项） | 再次删除 `metadata.json` | **再次**显示 BROKEN 告警（不卡在 正常态/CREATED）；DevTools Network 持续有 `/wf/list` 轮询（不中断） | 通过 | |
| E4 | 轮询不中断 | BROKEN 展示期间看 Network | `/wf/list` 仍按周期轮询（展示态不阻断列表轮询） | 通过 | |

---

## F. R5（D3）Resume 按钮

| 编号 | 场景 | 操作 | 预期 | 验证结果 | 问题 |
|------|------|------|------|---------|------|
| F1 | STOPPED 显示 Resume | Start 实例 → 点 Stop → STOPPED | 面板显示 **Resume** 按钮（连同 Reset） | 通过 | 问题：DAG界面点击Stop无反应。需在Session对话框中点停止 |
| F2 | Resume 续跑 | 点 Resume | 实例 → RUNNING（续跑，保留已完成任务/DONE）；DAG 恢复执行 |  未通过 | 按钮切回Stop之后，又切换回来。实际Session没有继续，还是停止状态 |

---

## 汇总

| 组 | 通过 | 失败 | N/A | 备注 |
|----|------|------|-----|------|
| A | | | | |
| B | | | | |
| C | | | | |
| D | | | | |
| E | | | | |
| F | | | | |

**总体结论**：□ 全部通过　□ 存在需修复问题（见下方问题列表）

**问题清单**（编号 + 现象 + 复现步骤）：

## 其他问题

（保留留白，供人工填写本版新发现的问题）

## 遗留（Iter-22 S1/S3/S4，未修，本版不验证）

- **S1** 自动 idle→stop 仍在：agent idle 时绑定实例会被置 STOPPED（含提问等待场景）。本版**预期仍存在**，无需复现。
- **S3** 孤儿进采用池完整语义未做：采用池可能不先对孤儿跑 `recoverOrphan` 解绑、或含未标注状态项。本版预期仍存在。
- **S4** reset 清会话对话未做：reset 后重跑 LLM 仍会对比旧对话。本版预期仍存在。

以上项在 Iter-21 之后继续（见 `development-plan.md` Iter-22）。
